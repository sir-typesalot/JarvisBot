let ACTIVE = false;

class Stocks{
    constructor() {};
    getQuote(company) {
        // get the most updated price of the stock
    };
    getWkHistory(company) {
        // get the wk record of the stock's Open/Close $
    };
}